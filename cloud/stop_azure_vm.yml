---
- name: Tasks
  hosts: localhost
  collections:
    - azure.azcollection
  connection: local
  gather_facts: no

  tasks:
  
  - name: Power On
    azure.azcollection.azure_rm_virtualmachine:
      resource_group: openenv-67smw-2
      name: SRVDEMO0001-AZURE
      started: no
